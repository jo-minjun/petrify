{
  "name": "petrify",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "dependencies": {
    "@petrify/core": "workspace:*",
    "@petrify/parser-viwoods": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
