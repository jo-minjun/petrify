{
  "name": "petrify",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "dependencies": {
    "@petrify/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
