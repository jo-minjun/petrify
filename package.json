{
  "name": "petrify",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "check": "biome check .",
    "check:write": "biome check --write ."
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "tesseract.js"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/pako": "^2.0.4",
    "@types/pngjs": "^6.0.5",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
